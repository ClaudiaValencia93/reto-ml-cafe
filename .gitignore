# entorno
.venv/
venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# datos derivados (los crudos si se versionan)
data/processed/*
!data/processed/.gitkeep
# estos dos si se versionan: 168 KB que permiten correr las pruebas de
# verificacion sin esperar los 8 minutos del pipeline completo
!data/processed/backtest_consumo.parquet
!data/processed/plausibility_audit.parquet

# artefactos
models/*.pkl
reports/figures/*.png
.env

# SO
Thumbs.db
desktop.ini

# artefactos de build (pip install -e)
*.egg-info/

# cache de pytest (se autoignora, pero se declara para que sea evidente)
.pytest_cache/

# el comprimido de entrega se genera, no se versiona
*.zip
